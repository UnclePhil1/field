# Dependencies / build
node_modules/
dist/
dist-ssr/
*.tsbuildinfo

# Env & secrets — NEVER commit
.env
.env.*
!.env.example
*.local

# Wallet keypairs / private keys — NEVER commit
*wallet*.json
my-wallet.json
*.key
*-keypair.json
id.json

# Firebase service account (secret) — NEVER commit
*firebase-adminsdk*.json
*-service-account*.json
wave-*.json

# Supabase local
supabase/.branches/
supabase/.temp/

# Editor / OS
.DS_Store
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
